# Deployment note

The old prototype deployment guide described automatic background cropping,
which is no longer the counting workflow. Use [`README.md`](README.md) for the
current manual-ROI setup, evaluation, Windows path, Render/Gunicorn, and
Vercel instructions.